import React from "react";
import './SearchBar.css'

const SearchBar = () => {
  return (
    <input 
      className="search-bar"
      type="search"
      placeholder="Search"
    />
  )
}

export default SearchBar